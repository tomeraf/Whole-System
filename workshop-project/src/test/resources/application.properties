spring.config.import=classpath:tests_config.yaml
spring.profiles.active=test

jwt.secret=TDNkeEc4qPSBelk6gSaCcc629o5XdyrX0ZmmWh/3LoQ=

# Database configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.h2.console.enabled=false

# System manager credentials - ensure these match what tests expect
startup.defaultSystemManager.name=idan
startup.defaultSystemManager.password=1234

# Turn off open in view to avoid lazy loading issues
spring.jpa.open-in-view=false